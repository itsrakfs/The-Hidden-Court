# The Hidden Court — ملخص المشروع الكامل (Handoff)

دي خلاصة شاملة لكل حاجة في مشروع موقع التصنيف بتاعك، عشان تحتفظ بيها أو تبعتها لأي حد/أي سيشن جديد يشتغل معاك، أو يقرأها opencode تلقائيًا.

---

## 1) إيه هو المشروع؟
موقع تصنيف دوري **بادل (Padel)** اسمه **The Hidden Court**.
- ليه الموقع ترتيب اللاعبين بالسير (ranking)
- كل لاعب عنده **بروفايل** فيه رسم بياني لسجل نقاطه (SVG) + إحصائيات
- صفحة **قواعد الدوري** بشكل جميل ومتحرك (أنيميشن)
- **لوحة إدارة** كاملة لتعديل النقاط واللاعبين

---

## 2) الموقع مكانه فين؟
| الحاجة | المكان |
|---|---|
| الموقع الحي | `https://thehiddencourt.fun` |
| المستضيف | Railway (رقم 1 مجاني، قرص مؤقت) |
| كود المشروع على GitHub | `https://github.com/itsrakfs/The-Hidden-Court.git` |
| الفرع اللي بيشتغل منه الموقع | `main` على GitHub (وبنرفع إليه من المحلي `master`) |
| فولدر المشروع على جهازك | `C:\Users\Lenovo\AppData\Local\Temp\opencode\the-hidden-court` |

**ملاحظة:** Folder المشروع الأصلي موجود في مسار مؤقت (Temp) —— لو الجهاز اتنضف ممكن يتشال. النسخة المحفوظة دائماً هي اللي على GitHub، فلو ضاع المسار، بنعمل `git clone` ونرجع تاني.

---

## 3) إزاي الموقع لو حصلت مشكلة في الـ server؟
Happens: **المولد بيشتغل بـ Python مباشرة** (مش gunicorn أو node).
- مجرد أن نبدأ من `server.py` هيشتغل بإعدادات تلقائية.
- التعديلات الوحيدة المطلوبة: إعادة تشغيل عملية python بعد أي تعديل في الكود.

لو عايز تعمل deploy جيّد: نعدّل الكود محليًا ← نرفع `git push origin master:main` ← Railway ياخد الـ main ويشتغل لوحده، وبيحصل في خلال ~1-2 دقيقة.

---

## 4) الداتا — الموضوع الأهم ⭐

### نظام الحفظ (لازم تفهمه):
- قاعدة البيانات المحلية هي `players.db` (ملف في فولدر المشروع).
- **مشكلة Railway:** مع كل deploy بيعمل قرص جديد فاضي، فأي بيانات بتتضاف على الموقع الحي **مش بيتمنحو الحفظ** عبر الـ deploys تلقائيًا.
- لذلك عملنا نظام **الـ seed**:

> `seed_players.json` هو **النسخة الرسمية المرفوعة في GitHub** — وكل إقلاع للسيرفر، لو جدول اللاعبين فاضي، بيرجع كل اللاعبين تلقائيًا من الملف ده.

**القاعدة الذهبية:**
> أي تعديل في النقاط/اللاعبين بيعمله المستخدم من لوحة الإدارة على الموقع الحي، **لازم بعدها نحمّل backup** (زرار "نسخ احتياطي" في `/admin`) ونستبدل فيه ملف `seed_players.json` ونرفعه على GitHub. وبكده أي deploy جاي يرجع بنفس البيانات بالظبط.

### ممنوع:
- ❌ حذف أو تغيير أي بيانات لاعب من غير ما المستخدم يوافق الأول (المستخدم حساس جدًا لبياناته).

### اللاعبين الحاليين (14 لاعب، وكلهم حاليًا **0 نقطة**):
1. Mohamed Reda
2. Waleed Allam
3. Anas Fayed
4. Eyad Eleterby
5. Marawan Elsaadany
6. Noor Elsherbeny
7. Ahmed Ramadan
8. Ali Maged
9. Mohamed Salah
10. Adel Eltahan
11. Makram Mahmoud
12. Ahmed Ayaad
13. Ahmed Hany
14. Zeyad Meshaal

> دي بداية جديدة نظيفة: **كلهم 0 نقطة وسجل الإحصاءات فاضي** — عشان تبدأ تضيف من أول وجديد، وبروفايل كل لاعب هيبدأ من غير أي "رجوع للصفر" في سجله.

---

## 5) حساب الإدارة والمصادقة
- **إعدادات الدخول المحلية:**
  - Username: `Rakfs`
  - Password: محفوظة في ملف `.env` المحلي (مش مرئي على GitHub)
- **على Railway:** إعدادات الدخول (`ADMIN_USERNAME` و `ADMIN_PASSWORD`) بتتحكم من **Environment Variables** في لوحة Railway نفسها.
- كلمة السر مخزنة دايماً كـ **hash** (مش بنحتفظ بصيغة نصية صريحة للمستخدم، باستثناء ما المستخدم يدخلها).

---

## 6) الـ Features الرئيسية في الموقع
- **الترتيب**: قائمة من 1 لأقل نقطة، مع صورة/بادج للثالث الأوائل.
- **الأسهم ↑ / ↓**: كل لاعب بيشير بأنه تحسّن أو انحدر خلال آخر 3 أيام (نافذة زمنية `TREND_WINDOW_SECONDS`).
- **بروفايل اللاعب**: `/player/<id>` — نقاطه، إحصائياته (matches، peak، season points)، وصفحة سجل التغييرات (نقاط قديمة ← جديدة).
- **صفحة القواعد**: `/rules` — تصميم مميز للدليل الرسمي بأنيميشن (بطاقات قواعد مطولة من الأسفل).
- **المعاينة/المشاركة**: مشاركة + إعدادات SEO + OG image.
- **لوحة الإدارة**: `/admin` — إضافة/تعديل/حذف لاعبين، تعديل النقاط، تحميل النسخ الاحتياطي.

---

## 7) قواعد الأمان (مهمة للـ dev)
- CSS/JS بيعملوا cache-busting: نقصد `?v=` في أي request؛ لو عدّلنا ملف static لازم نرفع رقم الإصدار في الـ templates الأول.
- صفحات HTML بتطلع `Cache-Control: no-store` (ممنوع حد يمسك نسخة قديمة).
- أصول static فقط بتتوّد caching طويل (max-age 30 يوم).
- `og:image` ثابت على `https://thehiddencourt.fun/static/og-cover.png` (مش `http`).
- **ممنوع** نرفع أي أسرار/باسوردات نصية على GitHub.

---

## 8) النسخة الاحتياطية (Backup) — إزاي؟
1. ادخل على `/admin` وسجّل الدخول.
2. دوس **"نسخ احتياطي"** (download) — هينزّللك JSON فيه كل البيانات + سجل النقاط لكل لاعب.
3. ابعتلي (أو لـ opencode) الملف ده.
4. أنا استبدل `seed_players.json` به وأرفعه على GitHub — وبكده أي deploy جاي بيحتفظ بكل التعديلات.

---

## 9) الوضع الحالي (آخر ما اتعدل)
- الموقع شغال والبقينت-14 متشافين على الموقع الحي، كلهم 0 نقطة.
- آخر commits إتسحبت على GitHub:
  - `24041b0` reset كل اللاعبين لـ 0 وسجل فاضي (بداية من جديد)
  - `e69171b` إصلاح الحفاظ على سجل النقاط عبر الـ seed
  - `dbaa7d8` إعادة تصميم صفحة القواعد + أنيميشن v6
  - `975760c` إصلاح مشكلة الكاش (لم تظهر اللاعبين على الكمبيوتر)، + no-store HTML
  - `c2e95b7` إصلاح `og:image` ليس `https`
  - `a1225df` تحديث seed من backup الموقع الحي
  - `a7d55a3` إضافة بروفايلات لاعبين + صفحة قواعد + OG image
  - `097d554` إضافة AGENTS.md (هذا الملف كمفصل أوتوماتيكي)
- كل التعديلات دي اترفعت على GitHub وكشفت الموقع الحي بعد الـ deploy.

---

## 10) إزاي تكمل شغل جديد؟
- اكتبلي المطلوب بالعربي (أو المكس) وخلّي الأوامر / الـ files تشتغل محليًا وتختبر الأول، وبعدها نرفعها.
- لما يكون فيه أي تعديل على النقاط/اللاعبين، تذكّر القاعدة الذهبية في **البند 4 و8** (احفظ backup → رجّع الـ seed → ادفع على GitHub).

---

*دا ملف الخلاصة — جاهز تبعته لأي حد، أو تحطه في Downloads عشان تسترجعه في أي وقت.*